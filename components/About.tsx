"use client";
import { bio,profile } from "@/data/content";import {useScrollReveal} from "@/lib/hooks";
export default function About(){const {ref,isVisible}=useScrollReveal(.1);return <section id="about" ref={ref as React.RefObject<HTMLElement>} className={`about-editorial section-pad reveal ${isVisible?"visible":""}`}><div><p className="kicker">About / 05</p><div className="about-copy">{bio.trim().split("\n\n").map(p=><p key={p}>{p}</p>)}</div></div><ul className="info-list"><li><small>Education</small>{profile.university}<br/>{profile.degree}</li><li><small>Academic</small>{profile.period}<br/>CGPA: {profile.cgpa}</li><li><small>Based in</small>{profile.location}</li><li><small>Focus</small>Java, DSA, full-stack development, AI-powered applications</li></ul></section>}
