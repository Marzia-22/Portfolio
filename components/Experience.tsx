"use client";
import {experience} from "@/data/content";import{useScrollReveal}from"@/lib/hooks";
export default function Experience(){const {ref,isVisible}=useScrollReveal(.1);const job=experience[0];return <section id="experience" ref={ref as React.RefObject<HTMLElement>} className={`experience-editorial section-pad reveal ${isVisible?"visible":""}`}><div><p className="kicker">Experience / 06</p><h2 className="experience-title">Making<br/>things real.</h2></div><div className="experience-body"><p className="experience-company">{job.role} <br/> {job.company}</p><p>{job.description}</p><ul>{job.highlights.map(h=><li key={h}>{h}</li>)}</ul></div></section>}
