"use client";
import{achievements,certifications}from"@/data/content";import{useScrollReveal}from"@/lib/hooks";
export default function Achievements(){const {ref,isVisible}=useScrollReveal(.08);return <section ref={ref as React.RefObject<HTMLElement>} className={`achievements-editorial section-pad reveal ${isVisible?"visible":""}`}><p className="kicker">Recognition / 08</p><h2 className="display-heading">Along the way.</h2><div className="achievement-grid">{achievements.map(a=><article key={a.title}><small>{a.event}</small><h3>{a.title}</h3><p>{a.detail}</p></article>)}{certifications.map(c=><article key={c.name}><small>Certification · {c.issuer}</small><h3>{c.name}</h3></article>)}</div></section>}
