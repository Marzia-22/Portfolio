"use client";
import{learning,skills}from"@/data/content";import{useScrollReveal}from"@/lib/hooks";
const words=[...skills.languages,...skills.frontend];
export default function Skills(){const {ref,isVisible}=useScrollReveal(.1);return <section id="skills" ref={ref as React.RefObject<HTMLElement>} className={`skills-editorial section-pad reveal ${isVisible?"visible":""}`}><p className="kicker">Capabilities / 07</p><h2 className="display-heading">Tools, systems,<br/>curiosity.</h2><div className="skill-marquee" aria-hidden="true"><div className="skill-marquee__track">{[...words,...words].map((w,i)=><span key={`${w}${i}`}>{w} ·</span>)}</div></div><div className="skill-groups"><div><h3>Backend / concepts</h3><p>{[...skills.backend,...skills.concepts].join(" · ")}</p></div><div><h3>Tools</h3><p>{skills.tools.join(" · ")}</p></div><div><h3>Currently exploring</h3><p>{learning.map(x=>x.label).join(" · ")}</p></div></div></section>}
